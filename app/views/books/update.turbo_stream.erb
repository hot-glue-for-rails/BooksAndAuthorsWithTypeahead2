<%= turbo_stream.replace "__#{dom_id(@book)}" do %>
  <%= render partial: 'line', locals: {book: @book } %>
<% end %>

<%= turbo_stream.update "flash_notices" do %>
  <%= render partial: "layouts/flash_notices", locals: {resource: @book} %>
<% end %>
